{
  "name": "face-it-numbers",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "description": "Chrome extension: CS2 map play/win rates during Faceit lobby veto",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "gallery": "vite --config vite.preview.config.ts",
    "gallery:build": "vite build --config vite.preview.config.ts"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.3",
    "@types/chrome": "^0.0.304",
    "typescript": "^5.7.3",
    "vite": "^5.4.14"
  }
}
